import Modal from "./Modal";
import styled from "styled-components";

const Container = styled.div``;

const App = () => {
  return (
    <Container>
      <Modal />
    </Container>
  );
};

export default App;
